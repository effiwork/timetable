import React, { Component as Cp } from "react";
import styles from "./Day.module.css";

export default class Day extends Cp{
    render() :React.ReactNode{
        return(
            <div>
                A Day
            </div>
        );
    }
}